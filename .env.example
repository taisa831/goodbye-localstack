AWS_DEFAULT_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
# Full-stack emulator (MiniStack, Floci, Kumo):
# AWS_ENDPOINT_URL=http://localhost:4566
# MinIO (S3 only):
# AWS_S3_ENDPOINT_URL=http://localhost:9000
# AWS_S3_USE_PATH_STYLE=true
# Optional resource names (probe defaults): COGNITO_USER_POOL_NAME, EVENT_BUS_NAME, S3_BUCKET_NAME,
# SQS_QUEUE_NAME, SNS_TOPIC_NAME, DYNAMODB_TABLE_NAME, SECRETS_MANAGER_SECRET_NAME, SSM_PARAMETER_NAME, SES_PROBE_EMAIL
